import classNames from "classnames";

const Footer = () => {
  return (
    <div id="Footer" className={classNames("animate-fadeIn","lb:pb-2 pb-1 lg:pt-6 pt-4 z-20")}>
      This is the Footer
    </div>
  );
};

export default Footer;
